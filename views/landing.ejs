<!DOCTYPE html>
<html>
    <head>
        <title>Cook Book</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"
        integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u"
        crossorigin="anonymous">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css" integrity="sha384-oS3vJWv+0UjzBfQzYUhtDYW+Pj2yciDJxpsK1OYPAYjqT085Qq/1cq5FLXAZQ7Ay" crossorigin="anonymous">
        <style type="text/css">
            #footnote {
                text-align: center;
                margin-top: 10%;
                margin-bottom: 3%;
            }

            #back {
                float: right;
            }

            .form-inline {
                text-align: center;
            }

            #search_term {
                width: 310px;
            }
        </style>
    </head>

    <body>


        <div class="container">
            <div class="jumbotron">
                <h1 id="main"><i class="fas fa-cookie-bite"></i> Hi, <%= user.username %></h1>

                <a href="/" id="back" class="btn btn-danger"><i class="fas fa-sign-out-alt"></i> Logout</a>
            
                <p>
                    What do you want to do today?
                </p>

                <form class="form-inline" action="<%= "/search/" + user._id %>" method="POST">
                    <div class="form-group">
                        <label class="sr-only" for="search_term">Search</label>
                        <input type="text" class="form-control" id="search_term" name="search_term" placeholder="Search for your favourite recepies" required>
                    </div>
                    <div class="checkbox">
                        <input type="radio" name="search_type" value="recepie" required> Recepie Name
                        <input type="radio" name="search_type" value="ingredient" required> Ingredient Search
                    </div>
                    <button type="submit" class="btn btn-default">Search</button>
                </form>

                

            </div>

            <div class="container">
                <div class="row">
                    <div class="col-sm-2"></div>
                    <div class="col-sm-5">
                        <h3>Want to see the trending recepies?</h3>
                        <% var url_index = "/index/" + user._id%>
                        <% var url_new = "/new/" + user._id%>
                        <% var url_my_questions = "/my_questions/" + user._id%>
                        <a href="<%= url_index%>" class="btn btn-primary"><i class="fas fa-book"></i> View Recepies</a>
                    </div>
                    <div class="col-sm-5">
                        <h3>Have an exciting new recepie?</h3>
                        <a href="<%= url_new %>" class="btn btn-primary"><i class="fas fa-plus-square"></i> Add Recepie</a>
                    </div>
                    <div class="col-sm-2"></div>
                </div>
                <br>
                <br>
                <div class="row">
                    <div class="col-xs-2"></div>
                    <div class="col-xs-8">
                        <div class="container">
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="container" id="footnote">
            <hr>
            Follow the code on <i class="fab fa-github"></i> <a href="http://www.github.com/utkarsh-raj">GitHub</a>
        </div>

        
    </body>
</html>